{
    "name": "package-helper",
    "version": "1.0.0",
    "description": "A package helper ... package :)",
    "scripts": {
        "build": "rimraf dist && babel src --out-dir dist --copy-files",
        "build:watch":
            "rimraf dist && babel src --out-dir dist --copy-files --watch"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "babel-cli": "6.24.1",
        "babel-polyfill": "6.23.0",
        "babel-preset-env": "^1.7.0",
        "colors": "^1.1.2",
        "cross-spawn": "5.1.0",
        "lodash": "4.17.10",
        "prettier": "1.8.2",
        "rimraf": "2.6.1"
    }
}
